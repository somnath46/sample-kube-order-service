server:
  port: 9090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/prep_dev
    username: prep_dev
    password: prep_dev
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    generate-ddl: true
    ddl-auto: create
    #    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        default_schema: public